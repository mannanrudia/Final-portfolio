.DS_Store
*.pyc
__pycache__/
.env
.vscode/
.idea/
*.log
# Keep the scripts for now unless the user wants them gone.
# But for a clean deploy, you could ignore some of the utility scripts if you want.
# I'll keep it simple for now and just hide the OS junk.
